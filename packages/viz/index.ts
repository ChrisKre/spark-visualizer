// packages/viz — chart primitives. React + D3 scales. No app state, no knowledge of the
// store. Real components (PartitionHistogram, TaskTimeline, PlanTree, ...) land in E5.
export const VIZ_PACKAGE_READY = true as const;
